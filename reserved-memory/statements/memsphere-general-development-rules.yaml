!statement
syntax: memsphere-20260721-stable
names:
  - memsphere-general-development-rules
  - 通用开发规范
defines:
  - 规定软件需求实施过程中的通用开发要求。
asserts:
  - 开发必须以已确认的需求、实施方案和开发计划为依据；需要改变已确认内容时，必须先完成同等确认。
  - 开发前必须了解受影响代码、现有架构和适用的仓库规范，并沿用合理的既有模式。
  - 修改必须限定在当前需求范围内，不得夹带无关重构或行为变更。
  - 实现必须完整处理当前需求涉及的正常路径、失败路径和边界情况。
  - 用户可见行为、公开接口、配置或使用方式发生变化时，必须同步更新相关文档、示例和契约说明。
  - 完成开发后必须记录修改范围、关键实现、行为与兼容性影响，以及仍未解决的问题。
sections:
  - !statement
    names:
      - 复杂度与失败处理
    defines:
      - 约束实现复杂度与问题实际影响、恢复成本之间的关系。
    asserts:
      - 系统不以消除所有错误为目标；面对异常时应优先守住数据、权限和结果正确性，能够清楚暴露并由用户或执行者通过重试、重启或重新执行解决的问题，可以作为正常失败路径。
      - 新增复杂度必须与问题的实际影响相称；不得仅为理论完备、全状态覆盖或极端路径自动恢复而扩大状态模型、跨层协议和维护范围，应结合实际影响、恢复成本和新增设计负担作出工程判断。
