当前 Artifact：{{artifactScope}}；Actor：{{actorId}}。
{{#if decision}}
{{#if decision.allowed}}允许：当前操作使用 {{decision.permission}} 权限。{{else}}拒绝：当前操作需要 {{decision.permission}}，但 Actor {{actorId}} 不具备该权限。{{/if}}
{{/if}}
{{#each permissions}}
- {{id}}：{{description}}
{{else}}
- 当前 Actor 在此 Artifact 下没有可执行的受控操作。
{{/each}}
