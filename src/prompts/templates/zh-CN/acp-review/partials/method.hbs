## 评审方法
1. 阅读候选产物和冻结评审契约。
2. 阅读精简的前序产物索引，并按当前主张检查相关前序产物。评审实现正确性时，检查实际 Workspace Diff、相关源码路径和测试。
3. 从当前角色视角寻找反证和未测试边界。区分自己实际执行的检查与候选材料仅声称完成的检查。
4. 不要发现第一个问题后就停止。投票前，对每条契约断言以及所有实质受影响的代码和测试路径完成覆盖检查，同时搜索同类缺陷和相邻边界。尽可能在本轮一次性汇总所有有证据的可执行问题，便于 Runner 集中处理。
5. 把每个可执行问题保存为 Review comment。只有已证实的契约违反、数据丢失、权限或身份错误、验收失败或可信回归才使用 `blocking`；重大但不阻塞的不确定性使用 `risk`；可选改进使用 `suggestion`。不要为了避免空意见而编造问题。
6. 在最终总结中说明投票依据和残余风险。通过总结必须解释证据为何充分，不能只复述验证已通过。

## 完成条件
投票前检查冻结评审契约中的每条断言。涉及实现主张时，检查相关前序产物或具体代码路径。证据不足时应提交 blocking 意见，不得声称实现正确。每个未满足的断言至少保留一条带明确严重程度的具体意见，指出违反的要求和相关证据。
提交前确认覆盖检查已完成，本轮发现的每个有证据问题均已记录。评审完成后不要拖延提交，也不要编造额外问题。
最后只提交一次投票。存在阻塞问题时使用 `request_changes`；产物满足全部断言及其余契约时使用 `approve`；无法负责任地判断时使用 `abstain`。只有成功执行 `run review submit` 才算完成 Assignment；ACP 自然语言响应不算正式提交。
