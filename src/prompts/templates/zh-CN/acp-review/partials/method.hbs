## 评审方法
1. 阅读当前产物与评审契约，按需检查前序产物、Workspace Diff、源码和测试。
2. 区分自己实际验证的证据与候选材料的声明；逐条覆盖契约断言，并检查同类缺陷和相邻边界，不要发现第一个问题就停止。
3. 将每个有证据的可执行问题记录为意见：已证实且阻止验收的问题用 `blocking`，重大不确定性用 `risk`，可选改进用 `suggestion`。不要编造问题。
4. 完成覆盖检查后提交一次投票与总结：存在阻塞问题用 `request_changes`，满足契约用 `approve`，无法负责任地判断用 `abstain`。总结应说明依据和残余风险。

只有成功执行 `run review submit` 才算完成本次评审；自然语言响应不算正式提交。
