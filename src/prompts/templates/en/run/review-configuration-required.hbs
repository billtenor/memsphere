Review configuration is required before this Run can start.
{{preflightJson}}

Save the example object as JSON, adjust its Policy and Slot bindings, then run:
memsphere run start <procedure> --review-config <path>
